anthropic>=0.40.0
openai>=1.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0
scikit-learn>=1.3.0

# Local HuggingFace inference (llm_judge_small.py)
transformers>=4.53.0
accelerate>=1.0.0
# torch must be installed separately — choose the build for your hardware:
#   CPU:        pip install torch
#   CUDA 12.x:  pip install torch --index-url https://download.pytorch.org/whl/cu124
# Optional: 8-bit quantisation (set HF_LOAD_IN_8BIT=1 in .env) — requires CUDA torch
#   pip install bitsandbytes
